let mongoose;_710‍.x([["default",()=>_710‍.o]]);_710‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String],
  publishedDate: { type: Date, default: Date.now },
});

const Post = mongoose.model('Post', PostSchema);
_710‍.d(Post);
